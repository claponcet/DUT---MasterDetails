<UserControl x:Class="WpfApp1.userControls.UC1imgLegende"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="50"
             x:Name="root">
    <Grid Height="Auto" MinWidth="50" MaxWidth="100">
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="50"/>
            <RowDefinition MinHeight="10"/>
        </Grid.RowDefinitions>
        <Image Source="{Binding NomImg, ElementName=root}"/>
        <TextBlock Grid.Row="1" Text="{Binding Legende, ElementName=root}" HorizontalAlignment="Center" FontSize="10"/>
    </Grid>
</UserControl>
